# Lyrics command
Finds lyrics of a given song. API provided by Some Random Api

# Setup
### Requirements:
- node-fetch
- discord.js embeds

### How to use
Just put it in your bots code. If you don't have a command handler which handles different arguments, this might not work.

Adjust it to whatever your bot runs on.
